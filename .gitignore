node_modules
dist
.wrangler
.dev.vars*
.env
.env.*

# รหัส Google Sheet ของ scripts/import-dorm-sheet.mjs — ไม่ใช่ตัวแปรของแอป จึงแยก
# จาก .dev.vars เพื่อไม่ให้ wrangler types ประกาศเป็น binding ของ Worker
scripts/.dorm-sheet.env
!.env.example
!.dev.vars.example
coverage
.commandcode/
.impeccable/
seed/live/

# บันทึกการทำงานภายใน (สเปก ตั๋วงาน บันทึกตรวจรับ) — ไม่ขึ้น repo สาธารณะ
# ประวัติ git ถูกล้างให้ไม่มีไฟล์กลุ่มนี้แล้ว การเพิ่มกฎนี้กันไม่ให้กลับเข้าไปอีก
.scratch/

# ไฟล์อ้างอิงที่เจ้าของหอวางไว้ให้ดู (สลิป/ใบแจ้งหนี้ตัวอย่าง) — มีข้อมูลผู้เช่าจริง
# จึงไม่ควรเข้า git
Resource/

# ภาพตัวอย่างที่เรนเดอร์จากข้อมูลจริง (มีชื่อผู้เช่าและยอดเงินจริง)
design/*.png
