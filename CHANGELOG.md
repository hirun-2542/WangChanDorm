# Changelog

รูปแบบของไฟล์นี้อิงจาก [Keep a Changelog](https://keepachangelog.com/th/1.1.0/)
และโปรเจกต์นี้ยึด [Semantic Versioning](https://semver.org/lang/th/) โดยเริ่มนับ
ที่ 1.0.0 เพราะตอนนี้ระบบใช้งานจริงกับหอหนึ่งแห่ง มีข้อมูลจริงและผู้ใช้จริง

## [Unreleased]

### Added

- **ปุ่มแจ้งเตือนบนแถบบน (กระดิ่ง) ใช้งานได้จริง** — เดิมเป็นปุ่มเปล่าที่มีมา
  ตั้งแต่ `design/index.html` ตอนนี้เก็บรายการ "บิลถูกปิด" ที่เข้ามาทาง WebSocket
  ระหว่างที่แท็บเปิดอยู่ พร้อมตัวเลขที่ยังไม่อ่าน · กดรายการแล้วไปที่บิลใบนั้นได้เลย
  · **ยังคงมี toast คู่กัน ไม่ได้แทนกัน** — toast บอกทันทีโดยไม่ต้องกดปุ่ม กระดิ่ง
  กันกรณีที่ไม่ได้จ้องจอตอนนั้น (toast หายใน 5 วินาที)
- เก็บในหน่วยความจำของแท็บเท่านั้น ไม่ลง `sessionStorage` โดยตั้งใจ: รายการเหล่านี้
  หมดความหมายหลังรีเฟรช (ของจริงอยู่ในหน้าบิล) และข้อมูลเก่าที่ค้างอยู่จะทำให้
  ตัวเลขบนปุ่มไม่ตรงกับบิล · กันซ้ำด้วย "ชนิด + id" และจำกัด 50 รายการ
- **แจ้งเตือนเมื่อมีผู้เช่าใหม่** ไม่ใช่แค่เมื่อมีคนจ่ายเงิน — ยิงจากสองทางที่ผู้เช่า
  เข้าระบบ: ลงทะเบียนเองผ่านฟอร์ม LIFF และเจ้าของกดเชื่อม LINE ให้ · ข้อความบอก
  ต่างกันเพราะสิ่งที่เจ้าของต้องทำต่อต่างกัน ("ลงทะเบียนเองผ่าน LINE" = ยังไม่รู้จัก
  ต้องเข้าไปดู · "เชื่อม LINE ให้แล้ว" = ยืนยันว่าเชื่อมติด) · ลิงก์ไปหน้ารายชื่อผู้เช่า
- เพิ่ม `tenantsChangedEvent` ให้หน้าผู้เช่าและแดชบอร์ดโหลดใหม่เมื่อมีผู้เช่าใหม่ —
  ก่อนหน้านี้ทั้งสองหน้าฟังแค่ `billsChangedEvent` จึงไม่มีทางเห็นคนใหม่ที่ลงทะเบียน
  จากอุปกรณ์ของเขาเองจนกว่าจะรีเฟรช

### Fixed

- **แผงแจ้งเตือนล้นออกนอกจอบนมือถือ** — ปุ่มกระดิ่งบนจอเล็กอยู่ซ้ายถัดจากโลโก้
  การยึดแผงกับปุ่มจึงล้น (ยึดขวา → ล้นซ้าย 250px, ยึดซ้าย → ล้นขวา 24px)
  จึงยึดกับขอบจอบนจอเล็กแทน วัดจริงที่ 390px: แผงอยู่ที่ 12–378 พอดีในจอ
- `notifications.ts` เคย import ชนิดข้อมูลจาก `realtime.ts` (โมดูลที่แตะ `window`)
  ทำให้ไฟล์ฝั่ง DOM ถูกดึงเข้า tsconfig ของ worker แล้ว typecheck พัง — ย้ายชนิด
  ข้อมูล `BillPaidNotice` มาไว้ที่โมดูลข้อมูลล้วนแล้ว re-export กลับให้ผู้เรียกเดิม

## [1.0.0] — 2026-09-24

รุ่นแรกที่ deploy ขึ้น production จริง (`wangchan-dorm`) พร้อม deploy ครบทั้ง
worker, migration และ secret เดียวกัน ไม่ใช่การ tag ย้อนหลัง

### Added

- **แจ้งเจ้าของทาง WebSocket เมื่อบิลถูกปิด** — Durable Object `FamilyRealtime`
  หนึ่งตัวต่อครอบครัว fan-out ไปทุกแท็บที่เปิดค้างอยู่ ทุกแท็บเห็น toast
  (`ห้อง … ชำระแล้ว … บาท`) ทันทีที่บิลถูกปิดจากที่ไหนก็ตาม และ **แท็บที่กดเองไม่
  ได้ toast ซ้ำ** เพราะ client ส่ง `x-realtime-id` ของ socket ตัวเองไปด้วยแล้ว
  เซิร์ฟเวอร์ข้าม socket นั้น
  - ครอบทั้งสามเส้นทางที่ปิดบิล: เจ้าของกดเอง (`mark-paid`), ปิดจากคิวรอตรวจ
    (`slips/:id/resolve`), และสลิปยอดตรงปิดอัตโนมัติ — ทุกใบที่ปิดจะ push
    LINE ไปหาเจ้าของด้วยการ์ดใบเดียว (`ownerBillPaidMessage`)
- **ลิงก์รูปสลิปแบบเซ็นชื่อและมีอายุ** (`/slips/p/:file?e=…&s=…`) — เซิร์ฟเวอร์
  และเบราว์เซอร์ใน LINE ไม่มีคุกกี้ของเรา ปุ่ม "ดูสลิป" จึงเปิดเส้นทางที่บังคับ
  session ไม่ได้ · ลิงก์ใหม่พิสูจน์สิทธิ์ด้วย HMAC-SHA256 ที่ผูกกับ `imageKey`
  และเวลาหมดอายุ (7 วัน) โดยยังเก็บเส้นทางเดิม (`/slips/:file` + session) ไว้ครบ
  · ถ้าไม่ตั้ง `SLIP_LINK_SECRET` การ์ดจะซ่อนปุ่ม "ดูสลิป" แทนที่จะส่งปุ่มที่กด
  แล้วเจอ 404
- **ปุ่ม "เปิดบิลในเว็บ" ใช้งานได้จริงจากในแชท LINE** — ปุ่มพาเจ้าของไปที่บิลใบ
  ที่ปิด พร้อมผ่าน `openExternalBrowser=1` เพื่อเปิดใน Safari/Chrome เพราะ Google
  ปฏิเสธ OAuth จาก embedded webview ของ LINE และที่หมาย (fragment) รอดข้าม
  Google OAuth ด้วยคุกกี้ `wangchan_google_next`
- **หน้าตั้งค่า: บอกว่าใครเชื่อม LINE อยู่ และเลิกเชื่อมได้** — แสดงชื่อบัญชีที่
  ผูกไว้ (แยก "ยังไม่ผูก" ออกจาก "ผูกแล้วแต่อ่านชื่อไม่ได้"), `DELETE
  /api/settings/owner-link` ล้างการผูกพร้อมรหัสเดิมในชุดเดียว
- **หน้าตั้งค่า: รายงานช่อง LINE ที่ใช้อยู่** (`GET /api/settings/line-channel`)
  — token ชี้ไป OA ตัวไหน, ตั้ง channel secret ไว้หรือยัง, และ **webhook ที่ LINE
  ตั้งไว้จริงชี้กลับมาที่ระบบนี้หรือไม่** พร้อมปุ่มตั้งให้ (จำกัดเฉพาะ endpoint
  ของ Worker ตัวเอง) · ตัวนี้จับเคสจริงที่ทำให้พิมพ์รหัสเชื่อมแล้วเหมือนไม่มีอะไร
  เกิดขึ้น: LINE ชี้ webhook ไป Worker อีกตัว
- **ปัดค่าน้ำ/ค่าไฟขึ้นเสมอสำหรับบิลที่ยังไม่จ่าย** (`migrations/0012`) — หอเก็บ
  เงินเป็นบาทถ้วน เศษจากอัตราที่มีทศนิยมจึงไม่ตกเป็นภาระของหอ · บิลที่จ่ายแล้วไม่
  ถูกแตะ เพราะเป็นการตกลงราคาที่เกิดขึ้นจริงแล้ว

### Changed

- **แจ้งเมื่อเจ้าของเปิดลิงก์ในเบราว์เซอร์ของ LINE** — หน้าเข้าสู่ระบบและหน้า
  คำเชิญตรวจจับ `Line/` ใน user agent แล้วขึ้นกล่องเตือนพร้อมปุ่มคัดลอกลิงก์
  แทนการปล่อยให้กดปุ่มที่ Google จะบล็อก
- **ลิงก์คำเชิญที่บอทส่งก็เปิดนอกแอป LINE** ด้วยเหตุผลเดียวกัน ส่วน **ลิงก์
  ลงทะเบียนจงใจไม่เติม** เพราะหน้านั้นต้องอยู่ใน LINE เพื่อใช้ LIFF และ access
  token (มีเทสต์กันไว้)
- แดชบอร์ดฟัง `billsChangedEvent` และรีเฟรชเองหลังมีบิลถูกปิดจากแท็บอื่น — เดิม
  ตัวเลขค้างจนกว่าจะรีเฟรชเอง

### Fixed

- **บิลที่ปิดแล้วเปิดจาก LINE ไม่เจอ** — ลิงก์เดิมไม่มี `period` แต่หน้ารายละเอียด
  หาบิลจากรายการของ **รอบบิลที่กำลังดูอยู่** (ค่าเริ่มต้นคือรอบปัจจุบัน) บิลเดือน
  ก่อนจึงขึ้น "ไม่พบบิลที่จะแสดง" ทั้งที่บิลมีอยู่ · `billDetailUrl` แนบ `period`
  ไปด้วยเสมอ
- ลิงก์บิลที่ไม่มี `period` และที่หมายที่หายหลังล็อกอิน Google ทั้งสองอย่างเกิดจาก
  การสร้าง URL คนละที่แล้วค่อย ๆ ต่างกัน จึงรวมมาเป็นตัวสร้างเดียว

### Notes

- Deploy รุ่นนี้ต้องมี `SLIP_LINK_SECRET` (ไม่มี = การ์ดยังส่งได้แต่ไม่มีปุ่มดูสลิป)
  และต้อง apply `migrations/0012` ก่อน — migration แตะเฉพาะบิลที่ยังไม่จ่าย
  (ยอดรวมของบิลที่ยังไม่จ่ายบน production เพิ่มขึ้น 4 บาทจากการปัดขึ้น)
- `durable_objects` + `migrations` ต้องประกาศทั้งระดับบนสุดและใน `env.demo` เสมอ
  เพราะ wrangler env ไม่สืบทอด binding ให้

[1.0.0]: https://github.com/hirun-2542/WangChanDorm/releases/tag/v1.0.0
